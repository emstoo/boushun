{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "boushun.rbacName" . }}
  labels:
    {{- include "boushun.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["nodes", "services"]
    verbs: ["list"]
{{- end }}
